/* The tsconfig.json file specifies the root files 
 and the compiler options required to compile the project. */
{
    "compilerOptions": {
     /* Additional Checks */
    "resolveJsonModule": true,
      "esModuleInterop": true     
    },
    "exclude": [
      "node_modules"
    ]
}